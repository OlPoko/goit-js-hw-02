
 function getSubscriptionPrice(type) {
    switch (getSubscriptionPrice) {
       case "professional":
          return 20;
          break;
    }
    switch (getSubscriptionPrice) {
       case "organization":
          return 50;
          break;
    }
         switch (getSubscriptionPrice) {
            case "starter":
               return 0;
               break;
         }
         switch (getSubscriptionPrice) {
            case "random":
               return "Invalid subscription type!";
               break;
         }
      switch (getSubscriptionPrice) {
    case "premium": 
      return "Invalid subscription type!" ;
      break;     
  }
}
